# 使用官方Node镜像
FROM node:20-alpine

# 安装依赖
RUN apk add --no-cache python3 make g++

WORKDIR /app

# 复制所有文件
COPY . .

# 安装依赖
RUN npm install

# 构建
RUN npm run build

# 暴露端口
EXPOSE 3000

# 设置环境变量
ENV HOST=0.0.0.0
ENV PORT=3000
ENV NODE_ENV=production

# 直接使用nuxi启动
CMD ["npx", "nuxi", "start"]