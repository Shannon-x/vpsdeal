# 使用Node 20的完整版本而不是alpine
FROM node:20-slim

# 设置工作目录
WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci --include=dev

# 复制项目文件
COPY . .

# 构建Nuxt应用
RUN npm run build

# 暴露端口
EXPOSE 3000

# 设置环境变量
ENV NODE_ENV=production
ENV NITRO_PORT=3000
ENV NITRO_HOST=0.0.0.0

# 启动命令
CMD ["node", ".output/server/index.mjs"]